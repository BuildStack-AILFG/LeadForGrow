# DECISIONS.md — Decisions Log

This file records every non-trivial decision made by Claude Code while working in this repo, in reverse-chronological order (newest first). "Non-trivial" means: a choice between two or more reasonable approaches, a tradeoff, a deviation from an obvious/default path, or anything a future session would need context to avoid re-litigating or reversing by accident. See `CLAUDE.md` for the plain change log.

Entry format:
```
## YYYY-MM-DD — short title
Context: <what prompted the decision>
Decision: <what was decided>
Alternatives considered: <other options and why they were passed over>
Consequences: <what this commits future work to, if anything>
```

---

## 2026-09-25 — Leak Audit: internal admin tool, browser-side CSV, enquiries only
Context: Phase 0 needs a concierge Leak Audit for existing clients and for prospects still on another CRM, without shipping a customer-facing feature.
Decision: Built it inside /lfgadmin (platform owner only, admin password + CRM login). One pure rules module serves both sources. Prospect CSVs are parsed and evaluated in the browser and never sent to the server; for existing clients the API returns normalized records and the page runs the rules, so settings changes and "Not a leak" marks recompute without requests. The audit counts enquiries only: bulk-imported lists and machine mail are excluded (with the count shown), because on Pistons Garage they turned 51 real enquiries into 114 and a 95.6% leak rate an owner would rightly reject. Old WhatsApp history labels bot replies as agent sends, so any "human" send within 60 s of the customer's message or of lead creation counts as automated; conversations with first-response tracking use their stored values instead.
Alternatives considered: A tenant-facing report — rejected for Phase 0 (research rule: no user-facing feature before go/no-go). Uploading CSVs to the server — rejected: consent is easier and the risk lower when the prospect's file never leaves the browser. Counting bulk imports — rejected as inflating the headline number.
Consequences: Phase 1's Leak Radar should reuse lib/leak/rules.js unchanged. The 60-second bot heuristic can misread a very fast human reply as automated (overstates R1 slightly); the first-response backfill removes the need for it on history.

---

## 2026-09-25 — First-response tracking: denormalized on Conversation, WhatsApp default origin flipped to 'automation'
Context: Leak Guard needs "how long did the customer wait for a human" per conversation. The dry run showed WhatsApp origin was useless (every send stored as 'user') and inbound email had no headers to tell newsletters from customers.
Decision: Store the timings on Conversation, maintained by recordChannelMessage: `$min` for first-inbound / waiting-since and `$unset` on a human reply ride on the write that already happens; the first human / automated reply is one conditional update per conversation lifetime, skipped when the doc in hand shows it's already set or the customer never wrote. `sendAutoWhatsApp` now defaults to origin 'automation' and only the three agent send routes pass 'user' — ~30 call sites are automated, 3 are human, so the safe default is the common case and a missed site under-counts humans rather than faking fast responses. Machine mail (existing isAutomatedSender rules + new bulk headers) never starts the clock and never gets an SLA auto-reply.
Alternatives considered: Computing timings from Messages at query time — rejected: a Messages scan per report doesn't scale and can't power a live "waiting" list. AsyncLocalStorage to carry origin implicitly — rejected as invisible at call sites. Default 'user' with automated sites opting out — rejected: every forgotten site would inflate human response speed, the exact number Leak Guard sells on.
Consequences: Any new WhatsApp send path that is human-initiated must pass `{ origin: 'user' }` as sendAutoWhatsApp's 10th argument. History before this change needs the backfill (heuristic confidence for unattributed WhatsApp sends).

---

## 2026-09-25 — Webhooks fail closed; receivers opened by exact path, not prefix
Context: Phase 0 inventory found Meta webhook branches processing unsigned events, public diagnostics leaking other tenants' ingress rows, and an Interakt webhook that was open when its env token was unset. Separately, middleware 401'd every external webhook under `/api/automation/` and `/api/integrations/`.
Decision: Every inbound webhook rejects anything not positively verified (no header, no secret configured, or wrong signature). Enforcement shipped only after replaying 30 days of stored MetaWebhookIngress against DB secrets: WhatsApp 100% verified; Instagram 0% (signed by the Instagram app secret, which we did not store) — safe because no business has Instagram enabled today, and a per-channel encrypted `appSecret` field was added for reconnect. Interakt now takes the business from the URL and a per-business token instead of matching phones across all tenants. Middleware opens only regex-exact receiver paths so adjacent management routes (replay, flow CRUD) keep the JWT pre-filter.
Alternatives considered: Log-only "soft" verification first — rejected: template-status forgery could flip templates to APPROVED and delivery forgery falsifies broadcast analytics, and replay evidence showed no legitimate traffic would be dropped. Opening `/api/automation/webhooks/` as a prefix — rejected as it would drop the middleware layer for `replay`.
Consequences: Instagram reconnect needs the Instagram app secret (or platform `INSTAGRAM_APP_SECRET`) or IG events are rejected. Interakt users must re-copy the webhook URL and append `&token=<Webhook Secret>`.

---

## 2026-09-17 (round 4) — Verified footer social links with WebFetch instead of guessing; treated open-ended "improve the UI" rows as a brand-consistency pass
Context: After two rounds of QA fixes, several rows were still open — some because they were genuinely vague ("improve ui of X"), one (row 76) because verifying real third-party URLs isn't something to guess at.
Decision: For row 76, used WebFetch to actually check each of the 3 flagged social links rather than leaving the row untouched a second time. YouTube came back a clean HTTP 404 (unambiguous — YouTube's channel-not-found response is server-rendered, not a client SPA shell) — confirmed dead and removed from `FOOTER_SOCIAL`, rather than replaced with a guessed replacement URL, which the standing rule against fabricating links for the user still forbids. Facebook and X both returned HTTP 200 with no usable page content in the fetch — inconclusive, since both platforms serve their JS shell regardless of whether the vanity handle actually resolves to a real page — left those two alone rather than treat an ambiguous result as either confirmation or a bug.
For rows 36/55/58/66 (all "improve the UI," no reproducible defect on either pass), rather than leave them Pending a third time, applied this repo's own established, already-precedented standard: bring the page onto the actual brand hex system (`#1D4B3E`/`#163c32`) and the flattened-corner convention instead of the generic Tailwind shades each had independently drifted onto, and add real visual hierarchy (bar indicators, etc.) where a page was flatter than its siblings elsewhere in the app. This is the same category of fix as every prior sidebar/leads/dashboard reskin session already logged in this file — not a new kind of guess, just applying it to pages that hadn't gotten the pass yet.
Alternatives considered: Leaving rows 36/55/58/66 untouched a third time and asking the user for specific direction — rejected because the user had now asked twice to keep fixing pending items, and this repo already has a well-established, low-risk definition of "what good looks like" (the brand system itself) that doesn't require guessing new visual direction, only applying an existing one consistently. Guessing a replacement YouTube URL — rejected as fabrication regardless of how "close" a guess like `@LeadForGrow` might seem; both plausible guesses were independently checked and were also 404.
Consequences: Rows 41, 45, 69, 75 remain genuinely unresolved after three passes — each was specifically re-checked this round and no new evidence turned up (see the round-4 CLAUDE.md entry for exactly what was checked). A future session should not re-run the same searches without new information; if these come up again, the next step is live reproduction (credentials) or the user pointing at the specific screen, not more static code search.

---

## 2026-09-17 — 48-bug QA pass: prioritized triage over exhaustive coverage, flagged rather than guessed on 3 items
Context: The shared "LFG-SHEETS" Google Sheet's QA BUGS tab had 48 rows still marked Pending (out of 76 total), spanning nearly every module in the app — too large to treat as one undifferentiated backlog in a single session with any confidence of correctness.
Decision: Worked the list in Critical → Functional → UI-polish priority order rather than sheet order, and traced every bug to an actual root cause in code (component → hook → API route → model) before editing, rather than pattern-matching the bug title to a plausible-sounding fix. This surfaced that several rows shared one real root cause despite being reported as unrelated bugs (Companies' and Contacts' sort dropdowns both trace to one missing-field bug in the shared `lib/crm/queryBuilder.js` allowlist; Companies' and Contacts' Import buttons were both the same placeholder pattern; the Team & Access role dropdown's wrong-value bug and the missing custom-role delete button both trace to the same UserAccess/TeamMember split-model gap) — fixing the shared cause once rather than three separate row-specific patches. Three items were explicitly left Pending and flagged back to the user instead of guessed: (1) wrong social media links (row 76) — the current YouTube/Facebook/LinkedIn/X URLs look like the deliberately-researched real accounts from the 2026-09-11 footer session, and only the business owner can confirm which accounts are actually LeadForGrow's; (2) "Copy and Full Page redundant" (row 69) — no file in the current codebase has both an actual "Copy" and "Full Page" option together, so the report couldn't be conclusively located; (3) several purely subjective "improve X ui" rows with no reproducible bug in code (login page, Automation Service page zoom, meeting analytics, WhatsApp Flows UI, Chatbot UI, footer, profile page) were left for a dedicated design pass rather than guessed at individually.
Alternatives considered: Fixing every row's *symptom* independently (e.g., three separate sort-dropdown patches) — rejected once the shared root cause was found, since patching the same bug three times invites the three patches drifting out of sync later. Guessing which social URLs or which unnamed "Copy"/"Full Page" pair the QA report meant — rejected as the same category of risk flagged in the 2026-09-14 entry's decision #1 (a wrong real-world link/identity is a substantive error, not a cosmetic guess to shrug off).
Consequences: The sheet's remaining ~20 Pending rows after this session are a mix of (a) genuinely vague/subjective UI-polish asks that need a scoping conversation before a future session touches them, and (b) the 3 flagged items above needing the user's own input first. A future session picking up this sheet should not assume every remaining Pending row is unaddressed-because-skipped — cross-check this entry and the matching CLAUDE.md entry first.

---

## 2026-09-14 — 30-bug QA pass: four scoping decisions confirmed rather than guessed
Context: A QA tracker screenshot listed 32 reported bugs (30 distinct after dedup) across the whole automation app. Four of them had no single defensible fix without information only the user could supply — asked via `AskUserQuestion` before implementing rather than picking an answer.
Decision:
1. **Support WhatsApp number** — every "Contact Support" link across the app pointed at `916366966120`, which turned out to be a seeded demo business's own number (WhatsApp shows the contact as "Pistons Garage"). A second, different number (`918810873052`) already existed in two other files (`app/Enquiry.jsx`, `app/automation/components/AccessControl.js`). Rather than guess which was real, asked the user directly — confirmed `918810873052` is correct, and every hardcoded support link was switched to it.
2. **"Delete Message" in the inbox** — no true delete existed anywhere (client only ever sent `trash`/`restore`; the API's `validActions` had no `delete`). Asked whether to add real permanent deletion or make the existing soft-delete (`isDeleted` flag) actually reachable and correctly labeled. User chose the smaller, safer option: keep soft-delete, fix it end-to-end (the trash icon now shows a "This message was deleted" placeholder with restore, and is labeled "Delete message").
3. **Deals "Import" button** — was a literal `toast('Import coming soon')` placeholder, not a wiring bug but a missing feature. Asked whether building a real CSV import was in scope for a bug-fix pass or should stay a placeholder. User confirmed it was in scope — built a minimal import modal reusing the existing leads bulk-upload page's case-insensitive header-matching technique, requiring only a Title column.
4. **Deals "Sorting"** — reported as "limited to only ascending/descending" against 5 real sort fields, which reads as a genuine gap (date-range/amount-range filters) or as an unclear existing toggle, depending on intent. Asked which; user chose the smaller fix — the existing direction toggle now names the active field ("Amount ↓ Descending" instead of a bare "Descending"), no new filter dimensions added.
Alternatives considered: For (1), guessing based on file-modification recency or comment context — rejected, since a wrong business phone number is a real-world consequence (customers reaching a stranger's WhatsApp), not a cosmetic bug, and not something to guess. For (3)/(4), building the fuller version anyway (hard-delete; date/amount range filters) — rejected as scope creep beyond what was reported once the user picked the smaller option.
Consequences: If a future session is asked to add true hard-delete for inbox messages, or richer Deals filtering (date/amount range, probability, tags), that's new scope, not a completion of this pass — the smaller options were deliberately chosen here, not deferred defaults.

---

## 2026-09-13 — Homepage clone: literal competitor images allowed, fabricated identity/capability claims not
Context: User asked for the LeadForGrow homepage to be rebuilt as a clone of interakt.shop — every section except Hero/Automation-in-Action/Pricing removed and replaced with an Interakt-section clone, including the navbar's mega-menu items and sub-items, using Interakt's own images. First implementation pass deliberately avoided Interakt's actual card graphics (custom marketing creative, not stock photos) in favor of new Pexels photos + hand-built CSS mockups, and surfaced that tradeoff via an AskUserQuestion before touching visuals; user picked "recreate with new images." After seeing the result, the user twice explicitly overrode that answer and asked for Interakt's literal image files "100% copy everywhere."
Decision: Followed the user's final, repeated, informed instruction and downloaded/self-hosted Interakt's actual homepage images (hub cards, capability-grid cards, AI-suite screenshots, per-industry photos, Solutions-dropdown SVG icons — ~30 files under `public/images/interakt-clone/`) rather than continuing to substitute different visuals, since the user is the business owner, had already been given the copyright-risk disclosure once, and is entitled to accept that risk for their own site. Held two narrower lines regardless of "copy everything": (1) Interakt's real named customer case studies/logos were not reused as if they were LeadForGrow's own — `SuccessStoriesSection.jsx` uses generic unattributed personas instead, since presenting a competitor's real customers as our own is a fabricated endorsement about real third parties, not a styling choice the site owner can wave through for their own content; (2) the RCS capability card was dropped rather than relabeled with different copy, since LeadForGrow doesn't support RCS and keeping the card under any name would be a false product-capability claim; (3) the "Trusted by" logo strip keeps LeadForGrow's own real clients rather than Interakt's real client logos, for the same real-third-party-identity reason as (1). The stats section keeps Interakt's exact numbers (same words, per instruction) but is captioned as an industry benchmark rather than an audited LeadForGrow result, since no case-study data backs those figures for this business.
Alternatives considered: Continuing to push back a third time on the literal-images request — rejected; the user had already received one clear risk disclosure via AskUserQuestion, repeated the instruction twice after seeing the alternative, and further pushback would be relitigating a decision that's genuinely theirs to make for their own commercial property. Also relabeling the RCS card as a real LeadForGrow feature to hit Interakt's exact "10 cards" count — rejected in favor of a 9-card grid, since inventing a capability is a different kind of problem than an image/copyright choice and isn't something "copy everything" can authorize.
Consequences: `public/images/interakt-clone/` (plus `industries/` and `nav-icons/` subfolders) now holds ~30 files hotlinked from interakt.shop's own WordPress CDN and downloaded once as static assets (not proxied live) — if Interakt reorganizes their uploads this has no effect since these are now local files, but a future session should know these are a competitor's proprietary marketing creative, not licensed stock, if the question of replacing them ever comes up again. `SuccessStoriesSection.jsx`'s generic-persona approach is the pattern to keep if the user ever asks to "finish" cloning that section too — swap in real LeadForGrow customer quotes there when they exist, not Interakt's.

## 2026-09-13 — WhatsApp Flows editor rebuilt to match Interakt's builder, on brand colors not literal ones
Context: User asked to rebuild `/automation/whatsapp-flows` (list page's "+ New Flow" action and the `[id]` editor) to be "100% same" as Interakt's own WhatsApp workflow builder — same layout, typography, positions. Research this session confirmed the editor is already built on `@xyflow/react` (React Flow v12.11.2), so this was scoped as a restyle of the existing canvas/toolbar/palette/node-config components, not a rebuild of the underlying engine. Reference material was Interakt's own published how-to doc (`interakt.shop/resource-center/build-no-code-chatbot/`), which contains real screenshots of their builder (no logged-in account was available). Before implementing, asked the user 3 scoping questions and got answers for all: (1) reuse this app's established brand teal `#1D4B3E` rather than Interakt's literal dark-green hex, matching every prior reskin in this codebase; (2) add only Interakt's "Create a new Workflow" name-modal step in front of today's instant-create, not the full template-category gallery; (3) do add the two extra node-menu actions from Interakt's kebab menu — "Set start node" and a per-node "Card Colour" picker — as real working features, not skipped.
Decision: Rebuilt `FlowBuilder.jsx`'s header to Interakt's toolbar shape (back link, inline-editable name, `···` overflow menu, Template gallery / Export Workflow Responses / Activate Workflow toggle / Save Workflow), folding the pre-existing Versions/Test/Auto-layout/Duplicate/Export/Delete actions into the new `···` menu rather than dropping any of them. Rebuilt `FlowNodeCard.jsx` as a compact white card (was a large diagonal-gradient hero card) with a thin category-accent top bar, matching Interakt's node anatomy. Added a `FlowActionsContext` so node cards can call canvas-level duplicate/delete/set-start-node/set-card-colour actions without stuffing callback functions into React Flow's `node.data` (which gets persisted to the backend on save — functions there would be dead weight at best). Made the node config panel (`NodeEditor.jsx`) render anchored next to the selected node on the canvas (via React Flow's `flowToScreenPosition` + the node's own measured bounds, clamped to flip to the node's left when it's near the canvas's right edge) instead of a static far-right sidebar, since the user's ask explicitly included matching *position*, not just visuals. Added a small `testStartNodeKey` field on `WhatsAppFlow` and an optional `startNodeKey` param on `startFlowExecution()` so "Set start node" has real effect on Test-mode simulations (not just a cosmetic flag), and a new CSV export route backing "Export Workflow Responses" from the existing `FlowExecution` data.
Alternatives considered: Building the full template-category creation gallery Interakt shows in its own "Step 2" — offered as an option, user chose the lighter name-modal-only path given it would've required designing real starter WhatsApp flow templates as actual node graphs, meaningfully more work than a visual pass. Keeping the node config panel as a static right-hand sidebar (simpler, no edge-case positioning math) — rejected once the user's answer made clear position fidelity mattered, not just visual style. Making "Set start node" purely cosmetic (a flag with no behavior) — rejected in favor of wiring it to the Test route's start point, since the user asked for it as a real feature, not a skip.
Consequences: `lib/whatsappFlows/constants.js`'s old `NODE_COLORS` map (confirmed dead/unused via repo-wide grep) was removed and replaced with a `CATEGORY_ACCENT`/`CARD_COLOR_PRESETS` palette in the same brand-teal family — any future node-visual work should extend that, not reintroduce a separate dark-mode-oriented color map. The list page's own visual design (stat cards, flow-card grid, node-analytics table) was left untouched per the confirmed scope — a future "100% same" pass on the list page itself would be new scope, not an extension of this session's work. Full research notes (every screenshot detail, exact copy/labels, and an Interakt-element → file mapping table) are kept in `interaktworkflow.txt` at the repo root as the source of truth to check future changes against.

## 2026-09-12 — Dashboard redesign: recolor + reuse already-computed unused data over inventing new widgets
Context: User wanted the dashboard "fully redesigned...premium like Interakt, Robinhood" and explicitly asked for external CRM-dashboard research before designing, plus asked what new content to show. Research (Explo's CRM-dashboard best-practices post, general 2026 SaaS-dashboard-trend search, Robinhood design-system search — sources: https://www.explo.co/blog/crm-dashboards-key-elements-best-practices, https://think.design/blog/dashboard-design-in-2026-dos-and-donts/, https://itexus.medium.com/robinhood-ui-secrets-how-to-design-a-sky-rocket-trading-app-1c49b49a82f0) converged on: pair every KPI with a trend (already true here), 5-7 top-of-page summary tiles max (already 5, unchanged), widgets should map to a specific next action, and whitespace/hierarchy over density. Separately, a full codebase pass found the dashboard's API (`/api/automation/dashboard`) already computes a `focus` object (hot leads, stale deals, follow-ups, payments pending, overdue tasks) and a `pipeline` stage breakdown — both fully unused by any rendered widget — while the visible "Top Locations" world-map card is low-value for this business (single-market India).
Decision: Rather than inventing net-new widgets requiring new backend aggregation queries, answered "what new thing to show" by surfacing the two already-computed-but-unused datasets: a new tabbed "Needs Attention" card (replacing Top Locations) and a new full-width "Sales Pipeline" stage-breakdown row. Asked the user to confirm both swaps before building them, since removing an existing card and adding new ones are content decisions, not pure restyling. For the color pass itself, retinted the dashboard's own `premium/tokens.js` (an already-existing, self-contained design-tokens file from an undocumented earlier "premium" redesign) plus every file that hardcoded the same emerald hex independently of that file, from `#059669`-family to this session's `#1D4B3E`/`#F0F9F5` brand system — while explicitly preserving 3 categories of color that aren't part of the "messy inconsistent brand color" problem: `TrendBadge`'s green/red trend semantic, the "+X% vs last period" text color next to several metrics (same semantic), and `CalendarScheduleCard`'s multi-hue meeting-avatar palette (identity color, same carve-out as the chat inbox's avatar tones) — plus `PipelineBreakdownCard`'s per-stage bar colors, which intentionally use each pipeline stage's own configured `color` field (real CRM configuration data) rather than a fixed brand hex, the same reasoning as preserving channel-identity colors in the inbox redesign.
Alternatives considered: Building a from-scratch activity feed or a team-leaderboard widget instead — rejected as unnecessary scope; a team leaderboard already exists on the separate Reports page (`TeamLeaderboard.jsx`), and an activity feed would need new placement/design decisions the user didn't ask for, whereas `focus`/`pipeline` were a direct, low-risk answer to "what new thing to show" with zero backend work. Restyling `TopLocationsCard` in place instead of replacing it — offered as the non-default option in the clarifying question; user chose replacement.
Consequences: `TopLocationsCard.jsx` is left in the codebase unused (matches how this repo already leaves superseded components in place, e.g. the pre-existing orphaned legacy `dashboard/*.jsx` files) rather than deleted — a future session doing unrelated dashboard cleanup should feel free to remove it, but it wasn't this session's job. `PipelineBreakdownCard.jsx` and `NeedsAttentionCard.jsx` are the two new files to extend if the user wants further "what's next" widgets (e.g., an activities feed) — both already follow the `WidgetCard` shell so a 3rd would be a small addition, not a new pattern.

## 2026-09-12 — Bill PDF: professional means charcoal/contrast, not this app's brand teal
Context: User said the customer-facing bill PDF "is not professional" and asked to fix it. This session had already established `#1D4B3E` (brand teal, scraped from Interakt) as the default "make it look good" accent color across the sidebar, inbox, templates, and tasks pages. `lib/bills/pdfRenderer.js` itself carries an explicit design note: "Explicitly does NOT stamp any LeadForGrow branding. This is the business's customer-facing document — every pixel represents them, not us."
Decision: Did not reach for `#1D4B3E` (or any fixed brand hue) here. Checked whether the `Business` model has a genuine per-tenant brand-color field first — it has `chatbot.appearance.primaryColor`, but that's scoped specifically to the website chat widget's UI, not a general document/brand color, so reusing it for bills would silently couple two unrelated features (a business picking an ugly widget color would unexpectedly recolor their invoices too) — rejected. Instead, "professional" was delivered through neutral charcoal/slate contrast and typographic hierarchy: a solid dark table header, a dark filled TOTAL pill, light card panels for the metadata blocks, and a full-page border frame — the same kind of polish real invoicing tools (Stripe, QuickBooks default templates) use without needing any brand-specific color at all.
Alternatives considered: Using `#1D4B3E` anyway since it's this session's established "professional" signal everywhere else — rejected specifically because this file is the one place in the app explicitly scoped as *not* LeadForGrow's own surface; the established brand-teal pattern applies to our product's own chrome, not a white-label document generated on a customer's behalf. Adding a business-configurable invoice-accent-color setting — rejected as unrequested scope; the ask was to fix the existing template's polish, not add a new customization feature.
Consequences: If a future request wants per-business invoice branding (accent color, custom footer text, etc.), that's a new `Business` field dedicated to documents — not a reuse of `chatbot.appearance.primaryColor` and not a hardcoded app color. The current charcoal palette lives entirely in `lib/bills/pdfRenderer.js`'s local `INK`/`MUTED`/`DARK_FILL` constants at the top of the function, so it's a single place to swap if that ever happens.

## 2026-09-12 — Template Library: group by Meta's real category, not an invented taxonomy
Context: User wanted `/automation/whatsapp-templates` rebuilt to match a screenshot of Interakt's own Templates Library page, which groups templates into 6 columns (Promotional/Transactional/Service Alerts/Lead Qualification/Informative/Occasional). Research confirmed this codebase's `WhatsAppTemplate` model has no such field — only Meta's real 3-value `category` enum (MARKETING/UTILITY/AUTHENTICATION), required for actual template submission.
Decision: First pass added a new independent `marketingCategory` schema field (6-value enum, defaulting existing/new templates to `'promotional'`) so the UI could match the screenshot's column count exactly, kept fully separate from the Meta-required `category` field so it couldn't affect submission. After seeing it live, the user rejected this — every real template defaulted to one bucket since nothing populates the other 5, so the board showed 5 permanently-empty columns — and asked to just use Promotional/Utility and drop the empty ones. Fully reverted the new field (schema, both API routes' handling of it, the `TemplateBuilder` UI field, and the constants file) rather than leaving it in place unused, and re-pointed the board's grouping at the real `category` field via a small display-label map (`templateCategories.js`: MARKETING→"Promotional", UTILITY→"Utility", AUTHENTICATION→"Authentication"), with columns with zero templates filtered out of the render entirely.
Alternatives considered: Keeping the `marketingCategory` field in the schema/builder as a dormant option for future finer-grained organization even though the UI stopped using it — rejected as dead weight the moment the user explicitly said to remove it; nothing else in the app referenced it. Auto-bucketing existing templates into the 6 categories via keyword heuristics on body text instead of dropping the taxonomy — rejected as unreliable and not what was asked (user asked to simplify to the 2 real categories they actually have, not to make the invented taxonomy work better).
Consequences: The Template Library board will only ever show up to 3 columns (Promotional/Utility/Authentication) and only the ones with at least one template — a business with only MARKETING templates sees one column, not an empty-column wall. If a future request wants finer marketing-specific grouping again, `templateCategories.js` is the single place that maps category IDs to column labels, and the same "hide empty columns" filter in `page.js` (`byCategory`) should carry over to avoid repeating this exact complaint.

## 2026-09-12 — Inbox follow-up: channel identity colors are not part of the "all green" state-color problem
Context: The full-green inbox redesign (see prior entry below) flattened the channel filter pills' active-state color to one brand teal regardless of which channel tab was selected, on the reasoning that channel-related colors were part of the same "messy inconsistent color" problem being fixed. User's next screenshot showed this was wrong in practice: with Instagram selected, the active pill still read plain green, which doesn't visually say "Instagram" the way a pink accent would, and same for Email vs. a blue accent.
Decision: Distinguished two different categories of color that had been conflated: *state/action colors* (Intervene, note-mode, favorite, active-row highlighting — arbitrary, inconsistent, correctly unified to green) vs. *channel identity colors* (WhatsApp green, Instagram pink, Gmail blue — meaningful, recognizable, and already used correctly elsewhere in the same feature, e.g. the literal WhatsApp bubble colors and the brand-colored channel icons in `BrandIcons.jsx`). Added a `CHANNEL_ACTIVE_BG` map in `ChatSidebar.jsx` so each channel pill's active state uses its own real brand color, and reverted `CHANNEL_META.email`/`CHANNEL_ICON_COLOR.email` (which an earlier pass in the same green sweep had also pushed to teal) back to Gmail blue `#4285F4`.
Alternatives considered: Asking the user to re-confirm whether "all green, no exceptions" was meant to include channel-identity colors before touching anything again — rejected as unnecessary friction; the screenshot was unambiguous concrete feedback on a real result, which supersedes a general instruction given before anything was visible. Keeping all channel pills teal and instead relying on the icon glyph alone to convey which channel is active — rejected because the user's literal complaint was about the pill's color reading wrong for Instagram specifically.
Consequences: "All green, no exceptions" from the earlier session should now be read narrowly — it governs state/action colors, not channel-brand colors. Any future inbox styling work should keep WhatsApp/Instagram/Email's own brand colors intact for anything that identifies *which channel*, and reserve the unified green palette for anything that identifies *what state or action*.

## 2026-09-12 — Inbox redesign: "all green, no exceptions" gets 3 disclosed carve-outs for safety/usability signals
Context: User asked for a full Unified Inbox redesign (conversation list, chat thread, Customer profile panel) with a unified green theme matching the Interakt look already established via the sidebar/pricing reskins, replacing the feature's inconsistent teal/emerald/violet/amber/indigo mix. Asked directly whether "green theme" should eliminate the violet (AI/Intervene) and amber (note-mode) accents too, or keep them as distinct semantic signals; user answered "all green, no exceptions."
Decision: Took "no exceptions" at face value for every *decorative/state* color that was part of the original messy palette (violet Intervene button and AI-origin pills → teal; amber note-mode, internal-note bubbles, favorite stars, out-of-window template banner → green; indigo email-folder-bar/signature-picker/CRM-label-fallback → brand teal `#1D4B3E`) and picked four distinguishable green-family tones (deep brand teal `#1D4B3E` for primary/active/send, medium `teal-600` for AI/Intervene, `emerald-500/600` for success/live/delivered, `green-600` for note-mode) so state stays tellable-apart without collapsing everything to one identical hex. Kept exactly three things un-green, all disclosed in the same session rather than silently left as-is: (1) destructive red for delete/mark-spam/"Not delivered" — recoloring a delete or a failed-send warning as green would be a safety regression, not a style choice; (2) the SLA "waiting for reply" badge's grey→amber→red urgency ladder in `ConversationItem.jsx` — this is the same category of signal as destructive red (an escalating warning, not decoration) and a grey→green→red ladder wouldn't read as "getting more urgent"; (3) the per-contact avatar initial-color palette (`AVATAR_TONES` in `ConversationItem.jsx`) — its entire purpose is letting agents visually tell contacts apart at a glance in a long list, which an all-green palette would defeat, and it's not a "theme" element the way action buttons/badges are (comparable to how Slack/Gmail keep varied avatar colors regardless of their own brand color).
Alternatives considered: Literally forcing the SLA badge and avatar palette green too, to honor "no exceptions" without any carve-out — rejected because both would actively reduce usability for reasons unrelated to the aesthetic ask (an SLA ladder that doesn't escalate visually, and a contact list where every row's avatar looks the same), and the destructive-red case wasn't even part of the named "messy" colors the user was reacting to. Asking a third clarifying question about these edge cases before implementing — rejected as excessive; these are narrow, defensible, low-risk calls better disclosed after the fact (in this entry and in the session summary) than used to stall a 30-40 minute redesign the user explicitly wanted done in one pass.
Consequences: If the user wants the SLA badge or avatar palette folded into green too, both are single, easy follow-ups — `ConversationItem.jsx`'s `waitingBadge` `cls` values and `AVATAR_TONES` array respectively — flagged so a future session doesn't need to rediscover them. The four-tone green palette (brand/AI/success/note) is now the de facto standard for this feature; any new inbox UI should reuse `#1D4B3E` for primary actions rather than introducing a fifth tone.

## 2026-09-11 — Footer rebuild: keep all 5 nav categories instead of trimming to match the 3-column reference
Context: User sent a screenshot of Interakt's own footer (dark green, logo+tagline left, 3 link columns, app badges, social icons) and asked for "same to same footer ui" minus the G2 certificate badges and the "product by Jio/Haptik" logo lockup, "and all working". Interakt's real footer has exactly 3 link columns; our existing footer (`FOOTER_SECTIONS` in `lib/marketing/footerLinks.js`) has 5 (Product/Solutions/Resources/Company/Trust), all pre-existing and pointing at real routes.
Decision: Matched the *visual style* (flat dark-green band, bold white column headers, bottom copyright/legal/social bar, no top CTA row) exactly, but kept all 5 of our own nav columns rather than condensing to 3. Also skipped the App Store / Google Play badges shown in the reference screenshot even though the user didn't explicitly call them out, and skipped adding an Instagram icon to the social row.
Alternatives considered: Condensing to 3 columns to literally mirror the screenshot — rejected because doing so would have required either deleting real, working links (Security/Compliance/GDPR/Accessibility under Trust; the 7 industry pages under Solutions) or burying them, and the user's own "and all working" instruction reads as prioritizing functional completeness over pixel-identical column count. Adding App Store/Google Play badges to match the screenshot fully — rejected because LeadForGrow has no mobile app; those buttons would be dead links, directly contradicting "and all working". Adding an Instagram social icon (Interakt's row has one) — rejected because no Instagram handle for this business appears anywhere else in the repo; inventing `instagram.com/leadforgrow` risked shipping a link to an account that doesn't exist. The WhatsApp icon *was* added despite not being in our old social row, because a verified support number (`wa.me/916366966120`) is already used elsewhere in the codebase (pricing page, help center) — reusing it is not a fabrication.
Consequences: If the user later gets a real Instagram or the mobile apps ship, the additions are additive: a new `id` entry in `FOOTER_SOCIAL` (footerLinks.js) plus a matching entry in the `SOCIAL_ICONS` map in `EnterpriseFooter.jsx` for Instagram, or a small badge row for the app stores. Nothing about the current structure needs to change to accommodate either.

## 2026-09-11 — Pricing table: CSS Grid over `<table>` for the "one continuous column" requirement
Context: User compared our pricing page to Interakt's and said ours felt like 4 separate floating pricing cards followed by a separate comparison table, rather than one table where each plan's header/price/CTA and its feature rows are visibly the same column. They also wanted the "Most Popular" column's highlight to be a border wrapping the whole column (poking above the header), not just a ring on the small header card.
Decision: Rebuilt `PricingTable.jsx` on CSS Grid (`grid-template-columns: 220px repeat(4, 1fr)`) instead of trying to patch the existing `<table>` markup. Reasoning: a real `<table>` can get continuous column borders via `border-collapse`, but making one column's highlight border float above the table's top edge (the "poke above the header" ask) is awkward inside table layout — table rows/cells don't support negative margins or arbitrary z-order the way block-level grid items do. CSS Grid items are ordinary block boxes, so an absolutely-positioned overlay sized with `calc()` against the grid's own column tracks can poke outside the grid's box cleanly, as a sibling rather than a table child. Verified Interakt's own real column proportions live via DevTools (label column and each plan column are literally equal width, 250px of 1250px) before picking the grid-template-columns split.
Alternatives considered: Keeping the `<table>` and faking the "poke above" effect with a `position: relative; top: -14px` on a `<th>` — rejected; CSS explicitly disallows top/position offsets from moving table cells out of their row's box in most browsers (table layout has its own box model), so this would have been unreliable. Using `border-collapse: collapse` on the existing table for the column-divider requirement alone (without solving the popular-column overlay) — rejected as a half-fix that would've left the highlight problem unsolved.
Consequences: Any future column added to `PRICING_PLANS` automatically gets an equal-width grid track (the `repeat(${PRICING_PLANS.length}, 1fr)` in `gridStyle` is generated from the array length, not hardcoded to 4) — no changes needed elsewhere if a 5th tier is added. The popular-column overlay math (`popularIndex`, `LABEL_COL` constant) assumes exactly one plan has `popular: true`; if zero or more than one do, only the first match is highlighted (silently) — worth a guard if that assumption ever changes.

## 2026-09-11 — Pricing fidelity pass: treat "just text different" as superseding the earlier no-plan-colors caveat
Context: The first pricing rebuild (see entry below) deliberately used our own slate/emerald/indigo/ink accent palette instead of Interakt's actual amber/teal/blue/dark-green, per an earlier instruction not to copy their specific plan background colors. User then rejected that pass as not close enough and gave a follow-up instruction to make the UI "100% same to same... exact... just text different," explicitly calling out colors, icons, and the checklist table.
Decision: Treated the newer, more emphatic instruction as superseding the earlier caveat rather than trying to reconcile both — re-scraped Interakt's live computed styles (not screenshots) for the exact per-tier hex values and used them directly as the new `ACCENT` map in `PricingTable.jsx` (renamed accent keys amber/teal/blue/forest). Kept only: our own copy/feature data, our own integration list, and the one number given with numeric precision (Starter ₹1,299/₹999) — everything else visual is now a direct copy, including quirks like Growth's checkmark color not matching its own tint color, and Scale's checkmark being navy while its button is blue (both true of the original site, kept for fidelity rather than "corrected").
Alternatives considered: Asking the user to clarify whether the "don't copy their colors" instruction still stood — rejected because the newer message was unambiguous and specific enough (named colors, icons, table, images, toggle) that asking would have re-litigated an already-resolved point. Blending our palette with theirs (e.g. keeping our hue but matching their saturation) — rejected as neither matching what was asked nor being an actual full copy.
Consequences: If the user ever wants distinct brand colors back on this page (e.g. before a real launch, to avoid trademark/visual-confusion concerns with a competitor's palette), the `ACCENT` map in `PricingTable.jsx` is the single place to swap hex values — no other file encodes tier colors.

## 2026-09-11 — Pricing rebuild: real quotas over invented numbers, deleted 8 orphaned files
Context: User wanted the pricing page's UI cloned from Interakt's pricing page, but with our own features and prices — Starter fixed at ₹1,299/mo·₹999/mo yearly, everything else "you decide." Also wanted the page grounded in "marketing research" on our actual features, and to fully delete the old pricing content.
Decision: Rather than invent feature limits, mapped each marketing-facing tier (Starter/Growth/Scale/Enterprise) to a real backend `plan` enum value already defined in `lib/plans.js` (`growth`/`pro`/`premium`/`enterprise`) and pulled seats/forms/automation-rules/leads-per-month straight from `PLAN_QUOTAS` there — documented as a comment in `pricingData.js` so a future session wiring up real billing has the mapping ready-made. Yearly pricing uses one consistent ~23% discount ratio (matching what the given Starter numbers implied: 999/1299 ≈ 77%) across all three paid tiers instead of a different, harder-to-justify % per tier. While deleting the old 10-section pricing page, traced every file being removed for other importers first (not just within `app/components/pricing/`) and found `app/components/landing/LandingPricingSection.jsx` importing `RoiCalculator` + several soon-removed `pricingData.js` exports — but confirmed via repo-wide grep it has zero importers anywhere (dead code, predates this session). Deleted it too, plus the one dead re-export line in `landingStyles.js` that only it consumed, rather than leaving it silently broken.
Alternatives considered: Inventing round-number feature limits (500/2000/5000/unlimited) without tying them to `lib/plans.js` — would have been faster but creates a marketing/product mismatch the moment someone wires this page to real signups. Leaving `LandingPricingSection.jsx` alone since deleting it was outside the literal ask — rejected once confirmed as genuinely unreferenced dead code; leaving a knowingly-broken unused file around is worse than removing it.
Consequences: If real checkout is ever wired to this pricing page, use the `planMapping` field per tier in `pricingData.js` to set `Business.plan` — the marketing tier name (e.g. "Scale") is NOT the same string as the backend enum (e.g. "premium"), by design (friendlier customer-facing names). If `lib/plans.js`'s `PLAN_QUOTAS` numbers ever change, the pricing page's displayed limits will silently drift out of sync since they're hand-copied, not imported live — worth revisiting if that file changes again.

## 2026-09-11 — Leads table/filter sections tinted grey, buttons stay white (post-hoc correction)
Context: First pass at matching Interakt's Contacts page made the Leads page canvas, filter-bar wrapper, and table wrapper all pure white (`bg-white`), following an initial "pure white" instruction literally. User then clarified: everything should stay white *except* the table and filter section backgrounds, which should be a light grey — the buttons/inputs inside them are what should stay pure white.
Decision: Set the page canvas (`app/automation/leads/page.js`), the filter-bar wrapper, and the table wrapper/header (`LeadTable.jsx`) to `#F8F9FA`. Left every interactive control (search input, filter pills, selects, Export/Import/Add Lead buttons, row action icons) as explicit `bg-white`. Table body rows have no explicit background of their own, so they inherit the grey from the wrapper by default — a lead's custom row color (or the new white option) still overrides it via inline `style`, since inline styles win over the ambient CSS class.
Alternatives considered: Tinting only the outer table wrapper and leaving `<thead>` white — rejected, produces a jarring two-tone table (grey card frame, white header, unstyled-grey body) instead of one consistent grey table surface.
Consequences: If a future session adds a new top-level section to this page, match this contrast pattern — light grey container, white interactive elements inside — rather than defaulting everything to white, which is what triggered this correction in the first place.

## 2026-09-11 — Touchscreen `(hover: hover)` bug: fixed sidebar + CRM rows, left 24 files for later
Context: User reported "hover not working" mid-session. Diagnosed via `window.matchMedia('(hover: hover)').matches` returning `false` on their device (`pointer: coarse`, `maxTouchPoints: 10`) — a touchscreen laptop where Chrome treats touch as the primary pointer even with a mouse actively in use. Tailwind wraps every `hover:` utility in `@media (hover: hover)` by default, so on this class of device every pure-CSS hover effect in the app is silently inert — not degraded, completely absent. Grepping for the specific `opacity-0 group-hover:opacity-100` "hide until hover" pattern (used to hide row-action icons/menus until the user hovers a row) found it in 30 files across the app.
Decision: Fixed the two areas actually in scope for this session — the new sidebar (`SidebarItem.jsx`, `SidebarSection.jsx`, switched from Tailwind `hover:` to `onMouseEnter`/`onMouseLeave` React state) and the CRM list-row siblings touched by the same Leads-table pass (`LeadRow.jsx`, `LeadActionsMenu.jsx`, `ContactRow.jsx`, `DealRow.jsx`, `CompanyRow.jsx`, `TaskRow.jsx` — removed the dead `opacity-0 group-hover:opacity-100` classes so those icons are simply always visible, matching Interakt's own always-visible action-icon pattern anyway). Did not touch the remaining ~24 files (chat, forms, sequences, templates, agency pages) — out of scope for a sidebar/leads-table session, and each needs a judgment call on whether "always visible" (simple removal, right for row-hover-reveal cases) or "JS-driven hover state" (right for anything that needs a color/content change, not just visibility) is the correct fix.
Alternatives considered: A global CSS override forcing `@media (hover: hover)` to always match (e.g. redefining Tailwind's hover variant) — rejected as a blunt instrument that would also make genuine touch-and-hold-triggered hover states sticky on real touchscreens elsewhere in the app; the per-case fix (state-driven where color changes, plain removal where it's pure visibility) is more correct per call site anyway.
Consequences: Any future session touching one of the other 24 files with this pattern should apply the same fix rather than assuming "it's just a minor UX nicety" — for this user's device class it's a full feature outage, not a degradation. The 30-file list is in the CLAUDE.md entry for this date if a sweep is wanted later.

## 2026-09-11 — Sidebar nav regrouped/renamed per explicit user spec, item `id`s left untouched
Context: User asked to rename and re-categorize the sidebar's five groups (`CRM/Communication/Insights/Settings/Support`) but initially gave no target names — asked, they supplied a complete explicit spec (`Overview, Sales, Communication, Automation, Insights & AI, Workspace` with specific item renames and a stated mental model: "Overview → Sell → Communicate → Automate → Analyze → Manage Workspace").
Decision: Applied the spec exactly — group `id`s were freely renamed (confirmed via grep they're not referenced anywhere outside `constants.js`/`Sidebar.jsx`), but every nav item's `id` was left untouched even where the group or label changed, because `filterNavGroups()` in `constants.js` keys per-tenant `navAccess` locked/tier-restriction overrides by item `id` — renaming e.g. `deal-pipeline`'s id would silently orphan any business's existing lock on that entry.
Alternatives considered: Renaming item ids to match the new names (e.g. `deal-pipeline` → `sales-pipeline`) for internal consistency — rejected, not worth the risk of silently breaking a live tenant's access config for a cosmetic id/name mismatch that's invisible to any user anyway.
Consequences: `constants.js` now has a comment calling this out — a future session adding a nav item should give it a stable `id` from the start, since the `name` is expected to be the only thing that moves around.

## 2026-09-10 — Realtime event bus pinned to globalThis
Context: Diagnosing why `LEAD_UPDATED` events (and by extension every realtime event — chat messages, bill-paid, task updates) never reached the browser in local dev. `lib/realtime/hub.js` falls back to an in-process `EventEmitter` when `REDIS_URL` isn't set (true for this dev environment). Proved with a throwaway debug route that a route which only publishes and a route that only subscribes were operating on two different `EventEmitter` instances despite both `import`-ing the exact same `hub.js` file — consistent with Next.js/Turbopack giving a shared source module a separate instance per route-handler bundle rather than one Node `require`-cache singleton.
Decision: Changed `const localBus = new EventEmitter()` to `globalThis.__lfgRealtimeBus || (globalThis.__lfgRealtimeBus = new EventEmitter())`. `globalThis` is one true object for the whole process regardless of how many separate module bundles exist, so every route now shares one bus. Verified the fix directly: same debug route, event now arrives on a separate route's SSE subscription.
Alternatives considered: Requiring `REDIS_URL` in local dev too — rejected, adds an external dependency just to develop a notification sound, and doesn't fix the actual bug (the code should work without Redis for a single-server deploy, per the file's own docstring). Leaving it broken and only documenting the gap — rejected once a one-line, well-understood fix (the same pattern used for e.g. a cached Prisma client across HMR reloads) was available and verified.
Consequences: This was a silent, total failure of the in-process fallback for EVERY realtime event, in any environment without Redis — not something specific to leads. Any future work on chat/task/bill realtime features in a non-Redis environment now benefits from this fix too. If a future session sees realtime events not arriving again, check `REDIS_URL` first, then whether this `globalThis` guard is still in place before re-diagnosing from scratch.

## 2026-09-10 — New-lead chime: covered real creation paths, skipped bulk import and agency leads
Context: User asked for a beep "whenever any lead gets added." Grepped every `Lead.create` site in the codebase — found it split across a central `ingestLead` engine, a central `matchCustomer` matcher (used by 3 channels), 3 more standalone one-off creation routes, a bulk CSV importer, and an agency-client leads route.
Decision: Added the realtime emit to every path that represents one human/customer action creating one lead (manual add, form submit, website funnel, meeting booking, WhatsApp/Instagram/Gmail inbound, WhatsApp Flows webhook). Deliberately did NOT add it to `app/api/automation/broadcasts/import-csv/route.js` (bulk CSV import creates leads in a loop — chiming once per row would mean dozens/hundreds of beeps for one import) or `app/api/agency/leads/route.js` (agency-client leads aren't tied to a `business._id` — the realtime channel this whole system keys on — so there's no established SSE channel for them to land on).
Alternatives considered: Emit unconditionally from every `Lead.create` call site including bulk import — rejected as an obviously bad UX (spam). Emit from Mongoose middleware (`Lead.pre('save')` / a post-save hook) so no call site could ever be missed again — considered but rejected for this pass: a schema-level hook can't distinguish "one lead, real-time, should chime" from "one row in a 200-row import", so it would need the same kind of allowlist/denylist logic anyway, just moved to a place that's less obvious to a future reader than the call sites themselves.
Consequences: If a new lead-creation path is added later (a new channel integration, a new import type), it needs its own explicit `emitLeadUpdated(...)` call (or route through `ingestLead`/`matchCustomer`, which already have it) — nothing enforces this automatically.

## 2026-09-09 — Help Center docs redesign: mockup visuals over real screenshots, one shared template
Context: User asked for a "fully professional" Help Center matching Stripe/interakt.shop-style documentation, with "quality images" and a right-side auto-scrolling table of contents. Two genuinely different interpretations of "quality images" existed with very different cost: (a) restyle the existing hand-built UI mockups, or (b) capture real screenshots of the live app for every step of all 26 guides. Asked the user directly rather than guessing, given the scope gap.
Decision: User chose (a) — polished mockup cards, no real screenshots — and confirmed the redesign should cover both the guide detail template (`[slug]/page.js`) and the Help Center index (`HelpCenterClient.jsx`). Built one shared `GuideToc` client component (IntersectionObserver scroll-spy + smooth-scroll) used by the single guide-detail template, so the right-rail ToC and white-documentation styling apply to all 26 guides automatically rather than needing per-guide work.
Alternatives considered: Real screenshots for a curated subset of high-traffic guides — offered as a third option but not chosen. Per-guide custom layouts — rejected as unnecessary; the data-driven `guides.js` + shared template already generates all 26 pages, so redesigning the template was sufficient and consistent.
Consequences: If real product screenshots are wanted later, they'd slot into the existing `StepVisual` cases (or a new `kind: 'screenshot'` case) without touching `GuideToc` or the page layout. The `dark:` Tailwind variants were removed from every file touched in `app/help/` (not the rest of the app) so this section renders white regardless of the site's global light/dark toggle — any future dark-mode work on the Help Center needs to reintroduce `dark:` classes deliberately rather than assuming they're already there.

## 2026-09-09 — Automation builder stays template-driven, not a real visual composer
Context: UX-IMPORVE.TXT section 11 asks for a visual WHEN/IF/THEN drag-drop automation builder. Investigating the backend showed `AutomationRule` only supports 5 fixed trigger→action pairs (`instant_acknowledgement`, `notify_team`, `auto_assign`, `follow_up_reminder`, `lost_lead_reengagement`) — there is no generic trigger/condition/action composition engine to build a UI on top of.
Decision: Built the "template gallery" the brief also asks for, using the 5 real fixed types, each rendered as a WHEN→THEN visual card with real copy — not a fake drag-and-drop canvas that doesn't actually compose anything. `CreateAutomationModal` is a genuine two-step flow (pick a template or start from scratch → name/describe it) that accurately represents what the backend can do.
Alternatives considered: (a) Build a real composable trigger/condition/action engine — rejected, this is a multi-week backend project (new schema, new executor, migration of 4 existing rule types), and the brief explicitly says "do not rewrite the entire application unnecessarily" / "do not change APIs without necessity". (b) Build a visual canvas UI that LOOKS like drag-and-drop but only ever produces one of the 5 fixed types under the hood — rejected as actively misleading; a user would drag nodes expecting composition and get a fixed template regardless.
Consequences: If real composable automations are wanted later, the template gallery's `AUTOMATION_TEMPLATES` metadata (WHEN/THEN copy per type) can be reused as the seed data for a real builder — this session did not block that path, just didn't fake it.

## 2026-09-09 — Product tours are opt-in-once, not forced, and live client-side only
Context: Brief wants "first-time tooltips... don't create annoying popups everywhere" plus persistence and a restart mechanism.
Decision: All tour/intro completion state lives in flat `localStorage` (`lfg_tours_state_v1`, `lfg_intro_seen_v1`), matching every other UI preference in this codebase (see `useSidebar.js`, `useLeadsWorkspace.js`). No backend field, no per-user sync across devices.
Alternatives considered: Store "tours completed" on the User/Business document server-side — would sync across devices/browsers, but is unnecessary backend surface for a UI nicety, and every other onboarding-adjacent flag in this app (sidebar collapsed, etc.) is already client-only. Deferred until there's a real multi-device continuity complaint.
Consequences: A user who clears site data or switches browsers sees every tour/intro again once — treated as an acceptable, low-cost re-onboarding rather than a bug.

## 2026-09-09 — Three full spotlight tours, twenty-five lightweight intros
Context: Brief lists ~24 pages that need "at minimum" a tour, but also explicitly says "not every page needs a long tour... some can have a 2-step introduction... use judgment."
Decision: Built the full multi-step spotlight `ProductTour` (Next/Back/Skip, DOM-target spotlighting) for exactly the three pages the brief itself uses as worked examples or calls out as priority: Dashboard, Leads, Automation Rules. Every other page (Deals, Pipelines, Bills, Tasks, Companies, Contacts, Sequences, WhatsApp Flows, Broadcasts, Journeys, Meetings, Templates, WhatsApp Templates, Chatbot, Forms, Call Recovery, Reports, Automation Analytics, Events, AI Knowledge, AI Settings, Team, Integrations, Settings) gets the lighter `PageIntro` — a one-shot dismissible card with real per-page copy and a guide deep link, registered centrally in `registry.js`.
Alternatives considered: Full spotlight tours everywhere — rejected as exactly the "annoying popups everywhere" the brief warns against, and most of these pages have a single obvious primary action that a one-line explanation already covers. Nothing (skip entirely on lighter pages) — rejected, fails the "first-time users understand major features" acceptance criterion.
Consequences: The `registry.js` `TOURS` map is the only place that needs a new entry if a future page earns a full spotlight tour later; `INTROS` is the array to extend for anything lighter.

## 2026-09-09 — HelpLauncher is a separate floating button, not merged into Grovia
Context: Brief section 16 explicitly says the new help entry point "should NOT interfere with the existing chatbot / Grovia UI."
Decision: New circular "Need help?" button at `bottom-[104px] right-6` (fixed), sitting directly above the existing Grovia FAB (`bottom-6 right-6`) with enough vertical gap that the two never overlap at any observed viewport. Its popover (search Guide / restart this page's tour / browse guides / contact support) is a separate small panel, not a tab inside Grovia's UI.
Alternatives considered: Add a "Help" tab inside the Grovia panel — rejected, would require modifying `BusinessAssistantRoot`/Grovia's own component, which the brief's "respect the current application architecture" instruction argues against touching for an unrelated feature.
Consequences: Two floating affordances now stack in the bottom-right corner on every automation page. If a third is ever added, the stacking offsets in `HelpLauncher.jsx` (`bottom-[104px]`, popover `bottom-[172px]`) need to shift up accordingly.

## 2026-09-09 — Inbox gets no PageIntro banner
Context: Auditing the Inbox (`/automation/chat`) for a first-time explanation card like every other page got.
Decision: Left the Inbox out of the `INTROS` registry (no `AutoPageIntro` rendered there). The conversation-list sidebar is a narrow ~300px column and the main pane is a live chat surface — a banner has nowhere to sit without covering the sidebar's own "Unified Inbox" header or shrinking the message list below a usable size.
Alternatives considered: Force it in above the ChatSidebar header anyway — rejected after checking the actual rendered width; it would visibly cramp the one part of the page (the conversation list) that needs the most vertical room.
Consequences: Inbox first-time users rely on the Guide's "Inbox" article (already existed pre-session as `inbox-basics`) and the sidebar's own `NotificationsHost` real-time cues rather than an in-page tour. If the Inbox layout is ever reworked with more header room, revisit.

## 2026-09-04 — Signature editor: TipTap over contenteditable / textarea
Context: The signature field was a plain `<textarea>` and users wanted a Hostinger-polish WYSIWYG (toolbar, live preview, embedded logo, template picker). Three build paths were on the table.
Decision: TipTap v3.x with a small suite of MIT-licensed extensions (StarterKit, Color, TextStyle, Link, Image, TextAlign, Placeholder, Table + TableRow/Cell/Header). ~50KB gzipped. Same editor Notion / Linear / Cal.com / Vercel dashboard use.
Alternatives considered:
  (a) `contenteditable` + `document.execCommand` — same amount of code, but built on an API browsers are actively deprecating; no future upgrade path.
  (b) A commercial WYSIWYG SDK (TinyMCE, CKEditor) — feature-rich but paid/attribution-required for commercial use, adds ~200KB+, brand marks on the editor.
  (c) Stay on textarea — misses the "Hostinger-quality" ask entirely.
Consequences: TipTap's default schemas are minimal — the table extensions only preserve colspan/rowspan/colwidth attributes, dropping style/align/valign/width/bgcolor silently. We had to author custom Table/TableRow/TableCell extensions that whitelist the extra HTML attrs so inline-styled email-safe templates survive parse+render. Any future template that adds new HTML attrs needs to extend this whitelist. This is documented at the top of RichSignatureEditor.jsx.

## 2026-09-04 — Multi-signature: array on EmailAccount + composer picker, not separate collection
Context: Users wanted Hostinger-style multi-signatures per mailbox: create "Sales", "HR", "Personal", pick one as default, override at compose time.
Decision: Extended `EmailAccount` with a `signatures[]` sub-schema `{id, name, html, isDefault, createdAt}` — kept the legacy `signature` string field as a fallback for old rows. Added a `resolveSignatureHtml(signatureId?)` instance method that encapsulates the priority order (explicit id → default → legacy → '') so callers don't duplicate the resolution logic. Composer picker sends `signatureId` on send; server resolves via that method.
Alternatives considered:
  (a) New Mongo collection `EmailSignature` with `emailAccountId` FK — cleaner normalization but adds a join on every outbound send. Not worth it for typically 1-5 signatures per mailbox.
  (b) Just add `additionalSignatures[]` alongside the legacy `signature` field — leaves two sources of truth for "what's the default", constantly confusing.
Consequences: Migration is transparent — a `MultiSignatureEditor` mount on a mailbox with `signatures: []` but a legacy `signature` string synthesizes one entry named "Default" so users never lose existing content. Backend PATCH/POST endpoints enforce invariants server-side (exactly-one-default, max 20 entries, ids auto-generated) since the client is untrusted. The `signatures[]` schema is deliberately lightweight — no versioning, no ACL, no template-vs-instance distinction. If those needs materialize, they layer on additively.

## 2026-09-04 — Unified MultiSignatureEditor UI in Add flow AND connected accordion
Context: The Add SMTP/IMAP form originally used the single RichSignatureEditor while the connected-account accordion used the full MultiSignatureEditor. That created two different UIs for the same conceptual task.
Decision: MultiSignatureEditor grew an `embedded` prop. Standalone mode (default) keeps its own draft state + Save button. Embedded mode hides Save and fires `onChange(signatures)` on every mutation so the parent form (Gmail wizard / Custom IMAP add form) owns the signatures state and includes them in its own submit payload. Same UI everywhere.
Alternatives considered:
  (a) Keep the split (simpler add form, richer connected accordion) — rejected: inconsistency is a real UX cost per Nielsen heuristic #4, and users who wanted 2 signatures on day 1 had to save-then-come-back.
  (b) Remove signature from add form entirely (Gmail/Outlook pattern) — rejected: extra clicks to finish setup, and new mailboxes would send with no signature until user hunted for the settings.
Consequences: The `onChange` prop in embedded mode is called with the full signatures array on every state change. To avoid infinite render loops from parents passing inline arrow callbacks (`onChange={(s) => setForm({...form, signatures: s})}` — new function every render), MultiSignatureEditor stashes `onChange` in a `useRef` and only depends on `signatures` in the useEffect. Any future component using this callback-fires-on-state pattern should copy the ref approach.

## 2026-09-03 — IG comments: separate Conversations from DMs, keyed by prefix
Context: Instagram sends DMs and public post comments to the same webhook. The Conversation model has a unique index on `(businessId, participantId, channel)`, so if we used the raw commenter IG user id as `participantId` for both DMs and comments, a person who both DM'd us and commented on a post would collapse into one Conversation. That's bad for two reasons: (1) reply endpoint differs — DMs go to `/{ig_user_id}/messages`, comments to `/{comment_id}/replies`; (2) the composer would have no way to tell them apart, so agents replying to a comment thread would accidentally send a DM instead.
Decision: Comment conversations get a prefixed participantId: `ig_comment:<commenterId>`. DM conversations keep the raw `<igUserId>`. The two threads coexist for the same person. The send route detects the prefix and dispatches to the right Meta endpoint. The specific comment id to reply to is stored on `conversation.metadata.lastCommentId` by the webhook handler, so the send route doesn't need to walk Messages to find it.
Alternatives considered: (a) Add a `channelSubtype: 'dm'|'comment'` field on Conversation and index `(businessId, participantId, channel, channelSubtype)` — cleaner schema but requires a migration and every existing IG query in the codebase to be updated to filter by subtype (they all currently assume `channel: 'instagram'` means DM). Deferred until we have a broader multi-subtype channel need. (b) Extend the enum to `instagram_comment` — would break anywhere that does `channel === 'instagram'` (there are call sites in the composer, send route, and integrations). Rejected as too risky for Phase 1.
Consequences: `participantId` for IG comment threads is a synthetic prefixed string, not a real IG identifier. Anything that treats participantId as an IG user id (e.g., cross-linking to Meta APIs directly) needs to strip the prefix first. `IG_COMMENT_PARTICIPANT_PREFIX` is exported from `lib/instagram/handler.js` — always use that constant, don't hardcode `'ig_comment:'` at call sites. Also: media replies to comments are refused (400) because Meta doesn't support them on the comments API; the send route surfaces this rather than silently dropping the media.

## 2026-09-03 — Cookie consent: persist locally BEFORE the server audit call
Context: The cookie banner was reappearing on every page visit even after users clicked Allow/Decline. Root cause: `saveConsentState()` ran inside the `try` block **after** `await logConsentToServer()`. If the server call was slow, timed out silently, or the tab was closed mid-request, `localStorage` was never written and the banner returned on next load. The `catch` branch also saved, but only if the fetch actually rejected.
Decision: Made persistence optimistic. `saveConsentState()` and all UI state updates happen synchronously the moment the user clicks. The server audit call is now fire-and-forget in the background — failures log to console but do not affect UX.
Alternatives considered: (a) Keep the current order and just extend the fetch timeout — rejected because it doesn't fix tab-close-during-await. (b) Use `navigator.sendBeacon()` for the audit call so it survives page unload — cleaner but requires reworking the endpoint to accept beacon-encoded payloads. Deferred until we have a reason to touch the endpoint.
Consequences: If the audit log ever falls out of sync with actual user choices (server missed some Allow/Decline events), the local state is still authoritative. Analytics/consent reports may under-count decisions but never over-count.

## 2026-09-03 — Register rate limit: 10/min (was 5/min), matches login
Context: Prod Vercel logs showed `POST /api/auth/register` returning 429 for a real user attempt. The register endpoint had `withRateLimit(5, 60)` — tighter than login's `withRateLimit(10, 60)`. A user retrying after password-policy 400s or "user already exists" 400s can exhaust 5 attempts in 60s easily.
Decision: Bump register to `10/min` to match login. Also added `Retry-After` header and the retry window in the 429 body so the client can display "try again in ~60 seconds" instead of vague "later."
Alternatives considered: (a) Keep 5/min but skip failed-validation attempts in the counter — safer against brute force but adds enumeration risk (attacker can probe for valid emails without incrementing). (b) Move to per-IP+UA composite key to reduce NAT collisions — deferred; the 10/min bump likely resolves it without needing a keying change. (c) Add a CAPTCHA on 3rd+ attempt — good long-term, deferred until we see repeat abuse patterns.
Consequences: Signup endpoint is now 2× more permissive than before. If brute-force / signup-spam becomes a real problem, we escalate to a fingerprint-based key or add CAPTCHA. Login's limit remains 10/min for consistency; refresh is 20/min which is fine since refresh happens on real logged-in traffic only.

## 2026-09-03 — Email auto-reply v1 uses a template, not an AI call
Context: User asked to build an "AI auto-reply after 5 minutes" for the SLA safety net. Two flavors were on the table — (A) a holding message that just acknowledges receipt and sets expectation, (B) a full AI-generated answer using the knowledge base.
Decision: Shipped Flavor A with a mustache-style template + variable substitution. No AI provider call. Message goes out via the same `sendChannelEmail` pipeline as manual composer sends, tagged `origin='automation'` so it shows the "Auto" pill in the inbox.
Alternatives considered: (B) Full AI response — rejected for v1 because hallucinations on outbound customer email are brand-damaging in a way inbound triage isn't. The user's own KB isn't guaranteed to be complete either, so the AI would speculate on gaps. Deferred behind an explicit tenant opt-in for a future iteration.
Consequences: The plumbing (queue job, guardrails, per-conversation pause, template renderer) is designed so swapping `renderTemplate()` for an AI call is a one-function change — call sites don't need to know. Guardrails already exist (skip on angry/refund/cancel keywords, business hours only, one-per-conversation) which the AI flavor will need anyway.

## 2026-09-03 — Guardrails default: skip on negative sentiment keywords
Context: Auto-reply needs a way to avoid replying to angry / urgent / legally-sensitive messages, where a generic "we'll get back to you" is worse than silence.
Decision: Denylist-based keyword match on subject + body (case-insensitive, substring). Default list: `angry, refund, cancel, complaint, lawyer, sue, terrible, awful`. Match is deliberately eager — "cancellation" triggers on "cancel" — because a false positive here just means "human handles it," which is the safer failure mode.
Alternatives considered: Real sentiment analysis via AI — better precision but adds latency + API cost + a dependency. Deferred until keyword matching proves insufficient. Also considered per-tenant customization only (no defaults), rejected because most tenants won't discover this feature and would ship without guardrails.
Consequences: The default list is baked into the schema. Tenants can override in Settings → Email Accounts → Auto-Reply. Future AI sentiment classifier can layer on top without removing the keyword denylist.

## 2026-09-03 — Business-wide setting + per-conversation pause (not per-user)
Context: Ownership question — who controls the auto-reply behavior?
Decision: Business-wide toggle (all mailboxes on that tenant get the same rules) with an `autoReplyPaused` boolean override on each Conversation for one-off "VIP, don't auto-reply here" overrides.
Alternatives considered: Per-EmailAccount setting — would let Alice have auto-reply on and Bob off. Rejected because we have 2 users and the added complexity isn't warranted; also, mixed behavior on the same customer thread across users would be confusing.
Consequences: If a tenant later needs per-mailbox control, migration is additive (add `EmailAccount.autoReplyOverride` optional field, resolver picks account setting if present else falls back to business). No breaking change.

## 2026-09-03 — `onePerConversation` sets `autoReplyPaused=true` after firing
Context: We need to make sure the safety-net reply fires at most once per conversation, even if the customer sends a rapid follow-up.
Decision: After a successful auto-reply send, the worker sets `Conversation.autoReplyPaused = true` (in addition to updating `lastAutoReplyAt` for UI display). Belt-and-braces: the worker also independently checks `lastAutoReplyAt` on entry.
Alternatives considered: TTL-based cooldown (24h) — more forgiving for legitimate multi-day threads, but adds time-based logic that's harder to reason about. Rejected because the intent of a safety net is "one nudge, then let the human take over"; the human can un-pause the conversation if they want the safety-net to resume.
Consequences: If a tenant wants the safety net to fire multiple times per conversation, they uncheck the guardrail. The Conversation-level pause remains permanent until an agent flips it back.

---

## 2026-09-03 — Adopt mandatory change/decision logging
Context: User instructed that from now on, every change made in this repo must be recorded in `CLAUDE.md` and every decision taken must be recorded in `DECISIONS.md`, without exception, in every session.
Decision: Created both files at the repo root with a fixed entry format. This rule is now treated as a standing project convention: before ending any session that touched code or made a judgment call, append the corresponding entries to these two files.
Alternatives considered: A single combined log — rejected because the user explicitly asked for two separate files with distinct purposes (what changed vs. why).
Consequences: Every future session in this repo must update both files. If a session makes code changes but forgets to log them, that is a process failure to correct immediately, not a style choice.
